
import { createTheme } from "@mui/material/styles";


const palette = {
  primary: {
    main: '#1f0655'
  },

};


export default palette;

export const appTheme = createTheme({
  palette: palette,

});
